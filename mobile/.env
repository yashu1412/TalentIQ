# Leave API_BASE_URL empty to let env.dart pick the correct URL based on platform.
# Android emulator: 10.0.2.2:8000/v1 (auto)
# iOS / other:      localhost:8000/v1  (auto)
# Override here to force a specific URL (e.g. a deployed server):
API_BASE_URL=
